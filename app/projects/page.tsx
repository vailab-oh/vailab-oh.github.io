import type { Metadata } from "next";
import { PageFrame } from "@/components/SiteShell";
import { allProjects, type Project } from "@/data/projects";
import { publications } from "@/data/publications";

export const metadata: Metadata = { title: "Projects", description: "Selected academic and research and development projects from VAI Lab.", alternates: { canonical: "/projects/" } };
function ProjectCard({ item }: { item: Project }) { const publication = item.related_publication_id ? publications.find((entry) => entry.id === item.related_publication_id) : undefined; return <article className="project-card"><div className="project-card-top"><p className="eyebrow">{item.group === "academic" ? "Academic research" : "Selected ADD R&D"}</p>{item.role && <span className="project-role">{item.role}</span>}</div><h2>{item.title}</h2><dl className="project-facts">{item.period && <div><dt>Period</dt><dd>{item.period}</dd></div>}{item.project_period && <div><dt>Project period</dt><dd>{item.project_period}</dd></div>}{item.involvement && <div><dt>User involvement</dt><dd>{item.involvement}</dd></div>}{item.supporting_organization && <div><dt>Supporting organization</dt><dd>{item.supporting_organization}</dd></div>}{item.program && <div><dt>Program</dt><dd>{item.program}</dd></div>}{item.program_authority && <div><dt>Program authority</dt><dd>{item.program_authority}</dd></div>}{item.project_management && <div><dt>Project management</dt><dd>{item.project_management}</dd></div>}{item.collaboration && <div><dt>International collaboration</dt><dd>{item.collaboration}</dd></div>}{item.performing_organizations && <div><dt>Performing organizations</dt><dd>{item.performing_organizations}</dd></div>}</dl><p className="project-area">{item.research_area}</p><div className="project-content">{item.content.split(/\n\s*\n/).filter(Boolean).map((paragraph, index) => <p key={index}>{paragraph}</p>)}</div>{publication?.url && <a className="project-publication-link" href={publication.url} target="_blank" rel="noreferrer">Related publication: {publication.title} <span aria-hidden="true">↗</span></a>}</article>; }
export default function ProjectsPage() { const academic = allProjects.filter((item) => item.group === "academic"); const add = allProjects.filter((item) => item.group === "add"); return <PageFrame><header className="section-shell projects-page-intro"><div><p className="eyebrow">Projects</p><h1>Research in practice.</h1></div><p>Selected research and development projects spanning autonomous systems, UAV technologies, artificial intelligence, and intelligent mobility.</p></header><section className="section-shell project-section"><div className="project-section-heading"><p className="eyebrow">Academic research projects</p><h2>Academic research projects</h2></div><div className="project-list">{academic.map((item) => <ProjectCard item={item} key={item.title} />)}</div></section><section className="section-shell project-section project-section-add"><div className="project-section-heading"><p className="eyebrow">Agency for Defense Development</p><h2>Selected R&amp;D projects at ADD</h2></div><div className="project-list">{add.map((item) => <ProjectCard item={item} key={item.title} />)}</div></section></PageFrame>; }
